#define THROTTLE_VERSION_MAJOR   ${THROTTLE_VERSION_MAJOR}
#define THROTTLE_VERSION_MINOR   ${THROTTLE_VERSION_MINOR}
#define THROTTLE_VERSION_PATCH   ${THROTTLE_VERSION_PATCH}
#cmakedefine THROTTLE_DEBUG
